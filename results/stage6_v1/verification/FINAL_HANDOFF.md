# Stage 6 final handoff

All six numerical queues completed. No numerical continuation is pending. Scientific limitations are in decision.json and report/REPORT.md; completion of execution does not establish a continuous floating interval or full strong-noise recovery.

Light reproduction from the project root:

```sh
ANNNI_PYTHON=/absolute/path/to/locked/python bash scripts/reproduce_stage6.sh --verify
```

The original deadline remains unchanged; any new numerical research requires a new run/configuration rather than silently extending this run. Extract the immutable Stage5 base, Stage6 code, and Stage6 data archives in that order. The external deliverables/STAGE6_CLEAN_EXTRACTION_CHECK.json records the final clean-extraction check.
