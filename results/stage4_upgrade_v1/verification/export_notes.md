# Export validation

The executed notebook has17 code cells and0 cell errors, including a freshly recomputed pure/density check. nbclient emitted a macOS psutil process-enumeration PermissionError during cleanup; execution and saved outputs completed successfully. This is not presented as a failed scientific cell.

The PDF was rendered to3 pages with ReportLab in a separate export environment; every page was visually inspected. Matched maps use fixed colors, absolute mitigation error scales agree, and source/limitation captions are retained.

The editable8-slide PPTX passes OfficeCLI schema validation, reports0 overflow/format issues, and has no leftover placeholders. An actual screenshot attempt failed: OfficeCLI found no headless browser. The skill's HTML-text fallback is used; slide DOM/text/geometry are inspected, while final viewer-specific contrast, fine overlap, exact gaps and column alignment are not visually verified. Numerical figure images have separately been visually inspected. No screenshot pass is fabricated.

Speaker notes contain772 English words, approximately5.94 minutes at130 words/minute. This is an estimated speaking time, not a timed human rehearsal.
