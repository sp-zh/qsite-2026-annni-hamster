# Release-only changes

New adapter scripts export inputs, rebuild statistics/figures, create notebook/report/slides and split V1/V2/V3 verification. No new scientific coordinate, candidate selection, detector fitting, threshold relaxation or large simulation was performed. Historical source modules and lock files are copied verbatim; an explicit recompute entry redirects only future opt-in outputs/deadline to build/.

Formal followup statistics use A_index union B_index:2718 records,110 distinct active coordinates; A has102, B45, with overlap.648 valid earlier attempts are excluded from formal pooling but preserved/costed in supplementary evidence. Existing cache-key, tie-rule, gate-fingerprint and monotonic-index corrections are retained. Main-map and window circuits at the same coordinate are never interchanged.

Figures use the same frozen D3 baseline and scientific definitions. Report/notebook/slides read release_statistics derived from numerical data, not copied prose. V1 reconstructs all saved repeated estimates. V2 actually recompiles and simulates a lightweight saved circuit, with a PennyLane crosscheck in the locked science environment. V3 uses one fixed record and original archived random identity. Detailed current receipts are separated by environment.

Historical reports are evidence, not new test execution. No research success claim follows solely from packaging. The repository has since been published to GitHub. The team confirms submitted: true and dashboard_status: draft; the dashboard entry is saved as Draft. See release_status.json.
